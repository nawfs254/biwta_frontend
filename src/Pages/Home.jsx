import React from 'react';
import HelmetTitle from '../utility/HelmetTitle';

const Home = () => {
    return (
        <div>
             <HelmetTitle title="Home"/>
        </div>
    );
};

export default Home;